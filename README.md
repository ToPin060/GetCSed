# GetCSed
"Get CSed" Discord application

## Setup

### Environment

```bash
# env/.env
    NODE_ENV=dev || prod

# env/.env.dev (or env/.env.prod)
    # TOKEN
    DISCORD_TOKEN=TOKEN

    # ID
    CLIENT_ID=ID
```

### Launch

```bash
# Install dependencies
npm install

# Launch application
npm run launch
```